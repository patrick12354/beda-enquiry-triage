# The slice runs fully offline with a mock LLM. These are only needed for live mode.
OPENROUTER_API_KEY=
OPENROUTER_BASE_URL=https://openrouter.ai/api/v1

# Never commit real values. In production these come from the secret manager,
# injected at runtime, scoped per integration. See docs/DESIGN.md section 6.
HUBSPOT_PRIVATE_APP_TOKEN=
SLACK_BOT_TOKEN=
WIX_WEBHOOK_SIGNING_SECRET=
